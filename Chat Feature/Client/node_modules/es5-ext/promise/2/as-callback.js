"use strict";

var ensurePlainFunction = require("es5-ext/object/ensure-plain-function")
  , ensureThenable      = require("es5-ext/object/ensure-thenable")
  , microtaskDelay      = require("es5-ext/function/#/microtask-delay");

module.exports = function (callback) {
	ensureThenable(this);
	ensurePlainFunction(callback);
	// Rely on microtaskDelay to escape eventual error swallowing
	this.then(
		microtaskDelay.call(function (value) { callback(null, value); }),
		microtaskDelay.call(function (reason) { callback(reason); })
	);
};
